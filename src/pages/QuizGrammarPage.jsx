import { Link } from 'react-router-dom'

// PLACEHOLDER — owned by Role C. Real page renders the grammar QuizSection.
export default function QuizGrammarPage() {
  return (
    <div>
      <h1>Grammar Quiz</h1>
      <p>QuizGrammarPage placeholder (Role C).</p>
      <Link to="/results">See Results</Link>
    </div>
  )
}
