// PLACEHOLDER — owned by Role C (UI). Add the real gradient/orb decor + styling.
// Role A only needs this to exist so App.jsx can render it once.
export default function BackgroundDecor() {
  return null
}
